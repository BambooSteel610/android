<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.checongbinh.baitapkethop" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ThemTacGiaActivity"
            android:label="Thêm Tác Giả"
            android:theme="@style/Base.Theme.AppCompat.Dialog"
            >

        </activity>

        <activity android:name=".ThemCaSi"
            android:label="Thêm Tác Giả"
            android:theme="@style/Base.Theme.AppCompat.Dialog"
            >

        </activity>

        <activity android:name=".ThemBaiHat"
            android:label="Thêm Bài Hát"
            >

        </activity>

        <activity android:name=".ThemSoVolActivity"
            android:theme="@style/Base.Theme.AppCompat.Dialog"
            android:label="Thêm Vol"
            >

        </activity>
    </application>

</manifest>
